apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    database: speedtest
    user: admin
    secureJsonData:
      password: adminpassword123
    jsonData:
      httpMode: GET
    editable: true
